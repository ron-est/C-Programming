// C's standard input and output library
#include <stdio.h>

int main(void)
{
    printf("To C, or not to C: that is the question.\n");
    // Indicates that the program "returns" the value 0 to the OS when it terminates
    return 0;
}
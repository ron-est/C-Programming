// Identify the directives and statements in this program
// What output does the program produce?

// This is a directive because it is a command intended for the preprocessor
#include <stdio.h>

// This is the main function declaration of the program
int main(void)
{
    // Anything in here is considered a statement which is executed when the program runs
    printf("Parkinson's Law:\nWork expands so as to ");
    printf("fill the time\n");
    printf("available for its completion.\n");
    return 0;
}